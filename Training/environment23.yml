name: ml-agents23
channels:
  - pytorch
  - nvidia
  - defaults

dependencies:
  - python=3.10.12 #above this mlagents complains
  - pip
  # - cudatoolkit=10.2
  - numpy=1.21.2  #in windows  Microsoft Visual C++ 14.0 is required for numpy 21.2. Get it here: https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170 and reboot
 # - protobuf=3.20
  - six
  - pip:
     - torch==2.8.0
     - mlagents==1.1.0
   
    # alternatively, to install locally:  
    # git clone --branch release_23 https://github.com/Unity-Technologies/ml-agents.git
    # cd ml-agents/ml-agents
    #  pip install -e .
